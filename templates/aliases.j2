{% for item in nullmailer_default_aliases + nullmailer_extra_aliases %}
{{ item.alias }}: {% if item.recipients | default(false) %}{{ item.recipients | join(', ') }}
{% else %}{{ item.recipient }}
{% endif %}
{% endfor %}
